services:
  app:
    build: .
    ports:
      - "5050:5050"

  db:
    image: postgres:14-alpine
    ports:
      - "5432:5432"
    environment: 
      POSTGRES_PASSWORD: secret_pass
      POSTGRES_DB: my_db 
