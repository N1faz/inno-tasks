apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - name: http
      port: 80
      targetPort: 5050
  type: NodePort
